#include <stdio.h>
/**
 * main - Entry Point
 *
 * Return: always 0 (success)
 */

int main(void)
{
	int n;
	int m;

	for (n = 48; n <= 57; n++)
	{
		putchar(n);
		n++;
	}
	for (m = 97; m <= 102; m++)
	{
		putchar(m);
		m++
	}
	putchar('\n');
	return (0);
}
